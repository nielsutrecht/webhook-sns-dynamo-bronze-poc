{
  "name": "webhook-sns-dynamo-bronze-poc",
  "version": "0.1.0",
  "description": "Webhook fan-out pipeline PoC",
  "main": "index.ts",
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc --noEmit",
    "generate": "AWS_PROFILE=personal AWS_DEFAULT_REGION=eu-west-1 npx tsx scripts/generate.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-sns": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@pulumi/aws": "^6.0.0",
    "@pulumi/pulumi": "^3.0.0"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.1030.0",
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  }
}
