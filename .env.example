# AWS profile to use for deployments
AWS_PROFILE=personal

# Must be set explicitly — Pulumi's S3 backend resolves region before the profile takes effect
AWS_DEFAULT_REGION=eu-west-1

# Pulumi passphrase for encrypting stack secrets (empty = no encryption, fine for PoC)
PULUMI_CONFIG_PASSPHRASE=
